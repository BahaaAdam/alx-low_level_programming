#include "main.h"

/**
 * *_strcat - a program that concatinates two strings
 * Return: 0 (success)
 */


char *_strcat(char *dest, char *src)
{

	char *dest[98];
	char *src[];


	for (i = 0, i <= 4, i++)

	{
		*dest[length 1 + i], *src[i];
	}

	return (0);
}
