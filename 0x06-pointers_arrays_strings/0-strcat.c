#include "main.h"
#include <string.h>

/**
 * *_strcat - a program that concatinates two strings
 * Return: 0 (success)
 */


char *_strcat(char *dest, char *src)
{

	char s1[98] = dest;
	char s2[] = src;
	length 1 = strlen(s1);
	length 1 = strlen(s2);


	for (i = 0, i <= 4, i++)

	{
		s1[length 1 + i], s2[i];
	}


	_putchar("%s", s1);
	return (0);
}
