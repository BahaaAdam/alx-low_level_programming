#include "main.h"

/**
 * main - a program that concatinates two strings
 * Return: 0 (success)
 */


int main(void)
{

	char dest[];
	char src;

	char *_strcat(char *dest, char *src);
	_putchar("%s\n", dest);
	return (0);
}
