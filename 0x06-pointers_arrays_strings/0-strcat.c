#include "main.h"
#include <string.h>

/**
 * *_strcat - a program that concatinates two strings
 * Return: 0 (success)
 */


char *_strcat(char *dest, char *src)
{

	for (i = 0, i <= 4, i++)

	{
		s1[length 1 + i], s2[i];
	}

	return (0);
}
