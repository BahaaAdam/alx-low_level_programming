#include "main.h"

/**
 * *_strcat - a program that concatinates two strings
 * @dest: destionation string 1
 * @src: source string 2
 * Return: 0 (success)
 */


char *_strcat(char *dest, char *src)
{

	char *dest[], *src[}, i, j;


	for (*dest = 0, dest[i] != '\0', i++);
	for (*src = 0, src[j] != '\0', j++, i++)
	{
		*dest[i], *src[j];
	}

	return (0);
}
